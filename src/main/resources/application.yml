server:
  port: 8081

spring:

  datasource:
    #url: jdbc:postgresql://localhost:5432/ofertaAp
    url: jdbc:postgresql://neondb_owner:npg_xpP1WT3ArlzL@ep-hidden-unit-ahlpyom2-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  redis:
    host: localhost
    port: 6379
  cache:
     type: redis
     redis:

      time-to-live: 3600000
        # Evita que se guarden valores nulos en la caché
      cache-null-values: false






jwt:
  secret: VGhpcyBJcyBBIFZlcnkgTG9uZyBTZWNyZXQgS2V5IEZvciBKV1QgVG8gU2F0aXNmeSAyNTYgQml0cw==
  expiration-ms: 3600000

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

    logging:
      level:
        root: INFO
        org.example.jwtRedisSwagger: DEBUG
        org.springframework.web: INFO
        org.springframework.security: DEBUG
        org.hibernate.SQL: DEBUG
        org.hibernate.type.descriptor.sql.BasicBinder: TRACE

      file:
        name: logs/application.log
        path: logs/

      pattern:
        console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
        file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50} - %msg%n"

      logback:
        rollingpolicy:
          max-file-size: 10MB
          max-history: 30
          total-size-cap: 1GB


